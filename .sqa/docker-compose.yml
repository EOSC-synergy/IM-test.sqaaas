services:
  imservice:
    entrypoint: /bin/sh
    command: -c "echo \"$$IM_AUTH\" > /tmp/auth.dat && sleep 6000000"
    image: grycap/im-client
    volumes:
    - source: ./
      target: /sqaaas-build
      type: bind
    working_dir: /sqaaas-build
    environment:
      IM_AUTH: |
        type = InfrastructureManager; username = micafer; password = alguna
        id = dummy; type = Dummy
version: '3.7'
